spring:
  datasource:
    url: jdbc:h2:mem:test;MODE=MySQL;
    username:
    password:

  mail:
    host: smtp.gmail.com
    username: sparestibot@gmail.com
    password: gvil bkva pdde cmdx
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect